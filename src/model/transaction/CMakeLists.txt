# Transaction Model - Blockchain transaction data structures and operations

# This file is included by the parent model CMakeLists.txt
# Individual model components don't need separate libraries
# since they're all part of the BlockChainModels target

# The source files from this directory are automatically
# collected by the parent GLOB_RECURSE command