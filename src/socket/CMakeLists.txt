# Socket Library - Network communication utilities for blockchain nodes
# Note: socket_util.c is actually in the utils directory and is part of BlockChainUtils
# This module only provides the socket executable programs (client.c and server.c)
# The socket utility functions are accessed via the BlockChain::Utils library

# This directory doesn't create a library, just provides the executable source files
# The executables are built in the apps/ directory